{
  "name": "testConsumeService",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^4",
    "express": "^4"
  },
  "devDependencies": {
    "sqlite3": "5.0.0"
  },
  "scripts": {
    "start": "cds run"
  },
  "cds": {
    "requires": {
      "ZAPI_RETURN_WP": {
        "kind": "odata",
        "model": "srv/external/ZAPI_RETURN_WP",
        "credentials": {
          "destination": "ZAPI_RETURN_WP"
      }
     }
    },
     "s4": {
        "kind": "rest",
        "credentials": {
          "destination": "abap-s4-basic"
        }
      }
}